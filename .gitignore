# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dev server process files written by start.sh / start.ps1
.incident-insight*.pid
.env
.env.*
!.env.example
backend/.env
# DuckDB ledger — keep the compressed seed in git; ignore the unpacked file

# DuckDB ledger — keep the compressed seed in git; ignore the unpacked file
backend/data/*
!backend/data/synthetic_ledger.duckdb.gz
backend/.venv/
.venv/
